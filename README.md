# Daily Rummikub Puzzle Generator & Solver

## Run the console demo

```powershell
node src/index.js
```

## Run the UI (static)

Serve the `web/` folder with any static file server, for example:

```powershell
npx serve web
```

## Run the UI (Node server)

```powershell
npm run ui
```
